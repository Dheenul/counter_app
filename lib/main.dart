import 'package:flutter/material.dart';
import 'MyApp.dart';
/* Create a main method of type void only to run our app */
void main() {
  /* Call the runApp method and the app file where our app is, as it's parameter */
  runApp(MyApp());
}
